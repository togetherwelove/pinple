# 고정 사이드바 및 독립 스크롤 V1

## 기준 문서

- `../인원 리스트 자동 그룹화 서비스 시스템 프롬프트 및 PRD V2.pdf`
- 2026-07-12 사용자 레이아웃 갱신 요청

## 적용 범위

- 대시보드 전용 래퍼를 `flex h-screen overflow-hidden`으로 구성한다.
- 전역 `layout.tsx`는 로그인과 랜딩 페이지에도 적용되므로 변경하지 않는다.
- 데스크톱 사이드바는 `h-full shrink-0 flex-col overflow-hidden`으로 높이와 너비를 고정한다.
- 데스크톱 사이드바는 기본적으로 펼쳐진 상태이며 사용자가 헤더의 아이콘 버튼으로 열고 닫을 수 있다.
- 펼친 사이드바 헤더에는 서비스 이름 `Pinple`을 표시하고 우측에 닫기 버튼을 배치한다.
- 프로젝트 목록 위의 보조 제목은 작은 텍스트 `최근`으로만 표시한다.
- 닫힌 사이드바는 `4rem` 너비의 축소형 레일로 유지하며 서비스 이름과 프로젝트 목록은 숨기고 열기 버튼은 유지한다.
- 축소형 레일에서도 계정 이니셜과 로그아웃 명령을 사용할 수 있다.
- 사이드바의 명단 목록만 `flex-1 overflow-y-auto`로 스크롤하며 계정 영역은 `mt-auto`로 하단에 남긴다.
- 메인 콘텐츠 열은 `flex-1 min-w-0 overflow-y-auto`로 독립 스크롤한다.
- 모바일은 기존의 상단 명단 탐색을 유지하고, 해당 콘텐츠 열만 스크롤한다.

## 검증 기준

1. 데스크톱에서 명단 결과를 스크롤해도 계정 및 로그아웃 영역이 화면 하단에 남아 있다.
2. 사이드바 명단이 길어져도 사이드바의 헤더와 계정 영역은 고정된다.
3. 메인 콘텐츠만 세로로 스크롤되며, 페이지 전체 스크롤바에 의존하지 않는다.
4. 사이드바를 축소하면 본문이 남은 너비를 채우고 열기 버튼으로 원래 너비를 복원할 수 있다.
5. 펼친 상태의 상단에는 `Pinple`, 프로젝트 목록 위에는 `최근`이 표시된다.

## 구현 및 검증 기록

- 2026-07-12: 대시보드 래퍼와 사이드바 클래스 적용 후 TypeScript 및 수정 모듈 ESLint 검사를 통과했다.
- 2026-07-12: 브라우저 자동 검증 세션은 로그인 페이지로 이동해 인증된 대시보드의 실제 스크롤 동작은 확인하지 못했다.
- 2026-07-20: 축소형 레일 방식의 사이드바 열기·닫기 정책과 헤더·최근 목록 표기를 확정했다.
- 2026-07-20: 데스크톱 사이드바에 `Pinple` 헤더, 열기·닫기 아이콘 버튼, `최근` 보조 제목과 `4rem` 축소형 레일을 구현했다.
- 2026-07-20: 축소 상태에서도 계정 이니셜과 로그아웃 버튼을 유지하고 본문이 남은 너비를 채우도록 적용했다.
- 2026-07-20: TypeScript `--noEmit`, 변경 파일 ESLint와 Next.js 프로덕션 빌드를 통과했다.
